context("Chunked Invoke Rows")

# add tests
test_that("tests on chunked-invoke-rows.R", {
  succeed(message = paste0("Skipping tests on chunked-invoke-rows.R.\n",
                           "Will add tests later."))
})
